<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AskMe – задай вопрос</title>
    <link href="https://fonts.googleapis.com/css?family=Nunito:300,400,700" rel="stylesheet">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag    'sass/style', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style="font-family: 'Nunito', sans-serif; background-color: #eee;">
    <!-- Fixed header -->
    <% if current_user.present? %>
      <%= render partial: 'application/header_logged' %>
    <% else %>
      <%= render partial: 'application/header_isnt_logged'%>
    <% end %>
    <!-- Printing Flash Message -->
    <%= render partial: 'application/flash' %>
    
    <%= yield %>

    <!-- Footer -->
    <%= render partial: 'application/footer' %>

    <!-- FIX NEXT TIME -->
    <!-- JS code -->
    <script src="https://code.jquery.com/jquery-3.1.1.min.js">
    </script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/js/bootstrap.min.js">
    </script>
    <!--Modal-->
    <script type="text/javascript">
      $('#myModal').on('shown.bs.modal', function() {
        $('#myInput').focus()
      })
    </script>
  </body>
</html>
